@{
    ViewBag.Title = "REPORTE VENTA POR PRODUCTO";
    Layout = "~/Views/Shared/_LayoutAdminTotal.cshtml";
}

<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <link href="~/Content/css/selectcomprim.css" rel="stylesheet" />
    <link href="~/Content/css/estilosfiltroscss.css" rel="stylesheet" />
</head>
<body>
    <div class="wrapper">
        <!--Inicio de sidebar-->
        <nav class="main-sidebar-custom-lg navbar navbar-expand navbar-white navbar-light">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" data-widget="pushmenu" href="#" role="button">
                        <i class="fas fa-bars"></i>
                    </a>
                </li>
                <li class="nav-item d-none d-sm-inline-block">
                    <a class="nav-link" href="@Url.Action("InicioAdminTotal","AdminPages")">Inicio</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                <!--aqui el icon de aumentar pantalla-->
                <li class="nav-item">
                    <a class="nav-link" data-widget="fullscreen" href="#" role="button">
                        <i class="fas fa-expand-arrows-alt"></i>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("CerrarSesion","Acceso")">Salir</a>
                </li>
            </ul>
        </nav>


        <!--Inicio de paginacion-->
        <div class="content-header">
            <section class="content-header">
                <!--Inicio de titulo-->
                <div class="container-fluid">
                    <div class="row mb-2">
                        <div class="col-sm-6">
                            <h1>REPORTE TAQUILLA</h1>
                        </div>
                    </div>
                </div>
                <hr/>

                <!--Inicio de contenido-->
                <div class="card">
                    <!--Inicio de Botones del sidebar de la tabla de contenido-->
                    <div class="card-header">
                        @using (Html.BeginForm("AdminPages", "filtrarTablaR", FormMethod.Post, new { id = "filtrarBusqueda" }))
                        {

                            <div class="row">



                                <div class="col-lg-2 col-md-4 col-sm-6 col-12">
                                    <label for="fechaInicioR">Fecha inicio</label>
                                    <input type="text" id="fechaInicio" name="fechaInicio" class="form-control" placeholder="dd/mm/aaaa" autocomplete="off">
                                </div>

                                <div class="col-lg-2 col-md-4 col-sm-6 col-12">
                                    <label for="fechaFinR">Fecha fin</label>
                                    <input type="text" id="fechaFin" name="fechaFin" class="form-control" placeholder="dd/mm/aaaa" autocomplete="off">
                                </div>
                                <div class="col-lg-2 col-md-4 col-sm-6 col-12">
                                    <label for="almacen">Almacen</label>
                                    <select class="form-control select2 almacen-select" style="width: 100%" name="almacen">
                                        <option value="">Seleccionar</option>
                                        <option value="211">Xunaan</option>
                                        <option value="162">Nohoch</option>
                                    </select>
                                </div>
                                <!--
    <div class="col-lg-2 col-md-4 col-sm-6 col-12">
        <label for="guia">Guia</label>
        <input type="text" id="guia" name="guia" class="form-control" value="">
    </div>
        -->

                                <div class="col-lg-2 col-md-4 col-sm-6 col-12">
                                    <label for="hotel">Guia</label>
                                    <select class="form-control select2 guia-select" style="width: 100%" name="guia">
                                    </select>
                                </div>

                                <div class="col-lg-2 col-md-4 col-sm-6 col-12">
                                    <label for="hotel">Hotel</label>
                                    <select class="form-control select2 hotel-select" style="width: 100%" name="hotel">
                                        
                                    </select>
                                </div>

                                <div class="col-lg-2 col-md-4 col-sm-6 col-12">
                                    <label for="hotel">Agencia</label>
                                    <select class="form-control select2 agencia-select" style="width: 100%" name="agencia">
                                       
                                    </select>
                                </div>

                                <div class="col-lg-2 col-md-4 col-sm-6 col-12 mt-2 mt-lg-0">
                                    <label for="btn-filtrar">Filtrar:</label>
                                    <div>
                                        <button type="submit" id="btn-filtrar" class="btn btn-info">
                                            <i class="fas fa-search"></i> Buscar
                                        </button>
                                    </div>
                                </div>
                            </div>
                        }

                    </div>

                    <!--Cuerpo del contenido(en este caso la tabla)-->
                    <div class="card-body">
                        <div class="table table-responsive">
                            <table class="table table-striped" id="tablaRemisiod">
                                <thead>
                                    <tr>
                                        <th>Folio Ticket</th>
                                        <th>Codigo de producto</th>
                                        <th>Descripcion</th>
                                        <th>PAX</th>
                                        <th>Hotel</th>
                                        <th>Agencia</th>
                                        <th>Guia</th>
                                        <th>Referencia</th>
                                        <th>Total</th>
                                        <th>Fecha</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <!--Aqui ira el contenido de la peticion
                                    por filtrado de busqueda
                                    -->
                                </tbody>
                                <tfoot>
                                    <tr>
                                        <th></th>
                                        <th></th>
                                        <th></th>
                                        <th></th>
                                        <th></th>
                                        <th></th>
                                        <th></th>
                                        <th></th>
                                        <th></th>
                                        <th></th>
                                    </tr>
                                </tfoot>
                            </table>
                        </div>

                        @section scripts {
                            <script>
                                $(document).ready(function () {

                                    //Aqui empieza la tabla

                                    var $filtrarBusqueda = $('#filtrarBusqueda');
                                    var $tablaRemisiod = $('#tablaRemisiod');


                                    // modificaciones de los selects fecha
                                    //, #fechaInicioRo, #fechaFinRo
                                    $("#fechaInicio, #fechaFin").datepicker({
                                        format: 'dd/mm/yyyy',
                                        language: 'es',
                                        autoclose: true,
                                        todayHighlight: true
                                    });

                                    $("#fechaInicio, #fechaFin").on("keyup", function () {
                                        var valor = $(this).val().replace(/\D/g, "").replace(/^(\d\d)(\d)/g, "$1/$2").replace(/\/(\d\d)(\d)/, "/$1/$2");
                                        $(this).val(valor.substring(0, 10));  // Limitar a 10 caracteres
                                    });

                                    $("#fechaInicio, #fechaFin").keypress(function (e) {
                                        // Permitir solo números y limitar a 10 caracteres (incluyendo '/')
                                        var charCode = (e.which) ? e.which : e.keyCode;
                                        if ($(this).val().length >= 10 && (charCode >= 48 && charCode <= 57)) {
                                            return false;
                                        } else if (charCode >= 48 && charCode <= 57) {
                                            return true;
                                        }
                                        return false;
                                    });

                                    // Evitar que se pegue texto no deseado
                                    $("#fechaInicio, #fechaFin").on('paste', function (e) {
                                        e.preventDefault();
                                    });

                                    // Inicializar Select2 Elements
                                    $('.select2').select2();

                                    // Inicializar Select2 Elements
                                    $('.select2bs4').select2({
                                        theme: 'bootstrap4'
                                    });

                                    $("select[name=almacen]").on("change", function () {

                                        let selectedAlmacen = $("select[name=almacen]").val()
                                        getGuidesByStore(selectedAlmacen)
                                        getHotelsByStore(selectedAlmacen)
                                        getAgenciesByStore(selectedAlmacen)

                                    })

                                    function getGuidesByStore(selectedAlmacen) {

                                        $("select[name=guia]").empty()

                                        if (selectedAlmacen !== null) {
                                            var guiaSelect = $(".guia-select");

                
                                            guiaSelect.prop('disabled', true).val('').change();

                                            console.log($("select[name=almacen]").val())

/*                                            <option value="">Seleccionar</option>*/
                                            var once = 0

                                            $.ajax({
                                                url: '/AdminPages/getGuidesByStore',
                                                type: 'POST',
                                                data: { almacen: selectedAlmacen, fecha1: $("input[name=fechaInicio]").val(), fecha2: $("input[name=fechaFin]").val() },
                                                success: function (data) {

                                                    //guiaSelect.find('option').not(':first').remove();
                                                    

                                                    if (Array.isArray(data)) {

                                                        

                                                         

                                                        $.each(data, function (index, item) {
                                                            if (item != "") {

                                                                if (once == 0) {

                                                                    // Agrega la opción de "Seleccionar todo" al principio
                                                                    guiaSelect.append($('<option>', {
                                                                        value: '',
                                                                        text: 'Seleccionar'
                                                                    }));
                                                                    once = 1

                                                                }

                                                                guiaSelect.append($('<option>', {
                                                                    value: item.nom_guia,
                                                                    text: item.nom_guia
                                                                }));
                                                            }
                                                        });
                                                         
                                                    } else {
                                                        Swal.fire({
                                                            icon: 'error',
                                                            title: 'Error',
                                                            text: 'Los datos no son una matriz válida.'
                                                        });
                                                         
                                                    }

                                                    guiaSelect.prop('disabled', false);
                                                },
                                                error: function () {
                                                    // Manejar el error de la solicitud AJAX si es necesario
                                                }
                                            });
                                        }
                                    }

                                    function getHotelsByStore(selectedAlmacen) {

                                        $("select[name=guia]").empty()

                                        if (selectedAlmacen !== null) {
                                            var hotelSelect = $(".hotel-select");


                                            hotelSelect.prop('disabled', true).val('').change();

                                            console.log($("select[name=hotel]").val())

                                            /*                                            <option value="">Seleccionar</option>*/
                                            var once1 = 0;

                                            $.ajax({
                                                url: '/AdminPages/getHotelsByStore',
                                                type: 'POST',
                                                data: { almacen: selectedAlmacen, fecha1: $("input[name=fechaInicio]").val(), fecha2: $("input[name=fechaFin]").val() },
                                                success: function (data) {

                                                    //guiaSelect.find('option').not(':first').remove();


                                                    if (Array.isArray(data)) {



                                                        

                                                        $.each(data, function (index, item) {
                                                            if (item != "")
                                                            {
                                                                if (once1==0) {

                                                                    // Agrega la opción de "Seleccionar todo" al principio
                                                                    hotelSelect.append($('<option>', {
                                                                        value: '',
                                                                        text: 'Seleccionar'
                                                                    }));
                                                                    once1=1

                                                                }
                                                                hotelSelect.append($('<option>', {
                                                                    value: item,
                                                                    text: item
                                                                }));
                                                            }
                                                        });

                                                    } else {
                                                        Swal.fire({
                                                            icon: 'error',
                                                            title: 'Error',
                                                            text: 'Los datos no son una matriz válida.'
                                                        });

                                                    }

                                                    hotelSelect.prop('disabled', false);
                                                },
                                                error: function () {
                                                    // Manejar el error de la solicitud AJAX si es necesario
                                                }
                                            });
                                        }
                                    }

                                    function getAgenciesByStore(selectedAlmacen) {

                                        $("select[name=agencia]").empty()

                                        if (selectedAlmacen !== null) {
                                            var agenciaSelect = $(".agencia-select");


                                            agenciaSelect.prop('disabled', true).val('').change();

                                            console.log($("select[name=agencia]").val())

                                            /*                                            <option value="">Seleccionar</option>*/
                                            var once2 = 0;
                                            $.ajax({
                                                url: '/AdminPages/getAgenciesByStore',
                                                type: 'POST',
                                                data: { almacen: selectedAlmacen, fecha1: $("input[name=fechaInicio]").val(), fecha2: $("input[name=fechaFin]").val() },
                                                success: function (data) {

                                                    //guiaSelect.find('option').not(':first').remove();


                                                    if (Array.isArray(data)) {



                                                         

                                                        $.each(data, function (index, item) {
                                                            if (item != "") {

                                                                if (once2 == 0) {

                                                                    // Agrega la opción de "Seleccionar todo" al principio
                                                                    agenciaSelect.append($('<option>', {
                                                                        value: '',
                                                                        text: 'Seleccionar'
                                                                    }));
                                                                    once2 = 1

                                                                }


                                                                agenciaSelect.append($('<option>', {
                                                                    value: item,
                                                                    text: item
                                                                }));
                                                            }
                                                        });

                                                    } else {
                                                        Swal.fire({
                                                            icon: 'error',
                                                            title: 'Error',
                                                            text: 'Los datos no son una matriz válida.'
                                                        });

                                                    }

                                                    agenciaSelect.prop('disabled', false);
                                                },
                                                error: function () {
                                                    // Manejar el error de la solicitud AJAX si es necesario
                                                }
                                            });
                                        }
                                    }


                                    $tablaRemisiod.DataTable({
                                        processing: true,
                                        responsive: true,
                                        "autoWidth": false,
                                        language: {
                                            "processing": '<i class="fa fa-spinner fa-spin fa-3x fa-fw"></i><span class="sr-only">Loading...</span> ',
                                            "sLengthMenu": "Mostrar _MENU_ registros",
                                            "sZeroRecords": "No se encontraron resultadosx",
                                            "sEmptyTable": "Ningún dato disponible en esta tabla",
                                            "sInfo": "Mostrando registros del _START_ al _END_ de un total de _TOTAL_ registros",
                                            "sInfoEmpty": "Mostrando registros del 0 al 0 de un total de 0 registros",
                                            "sInfoFiltered": "(filtrado de un total de _MAX_ registros)",
                                            "sInfoPostFix": "",
                                            "sSearch": "Buscar:",
                                            "sUrl": "",
                                            "sInfoThousands": ",",
                                            "sLoadingRecords": "Cargando...",
                                            "oPaginate": {
                                                "sFirst": "Primero",
                                                "sLast": "Último",
                                                "sNext": "Siguiente",
                                                "sPrevious": "Anterior"
                                            },
                                            "oAria": {
                                                "sSortAscending": ": Activar para ordenar la columna de manera ascendente",
                                                "sSortDescending": ": Activar para ordenar la columna de manera descendente"
                                            }
                                        },

                                        "dom": 'Blfrtip',
                                        "lengthMenu": [[10, 50, 100, 1000, 2000, -1], [10, 50, 100, 1000, 2000, "Todos"]],
                                        "pageLength": -1,
                                        scrollX: true,
                                        buttons: [
                                        ],
                                         
                                        buttons: [

                                            {
                                                extend: 'excel',
                                                className: 'btn btn-excel rounded-0',
                                                text: '<i class="fas fa-file-excel"></i> Excel',
                                                filename: function () {
                                                    // Obtiene la fecha y hora actual.
                                                    var now = new Date();

                                                    // Formatea la fecha actual en dd-mm-yyyy.
                                                    var day = ("0" + now.getDate()).slice(-2);
                                                    var month = ("0" + (now.getMonth() + 1)).slice(-2);
                                                    var year = now.getFullYear();
                                                    var dateString = day + '-' + month + '-' + year;

                                                    // Convierte la hora a formato de 12 horas con AM o PM.
                                                    var hours = now.getHours();
                                                    var minutes = now.getMinutes();
                                                    var ampm = hours >= 12 ? 'PM' : 'AM';
                                                    hours = hours % 12;
                                                    hours = hours ? hours : 12; // La hora '0' debería ser '12'.
                                                    minutes = minutes < 10 ? '0' + minutes : minutes;
                                                    var timeString = hours + ':' + minutes + ' ' + ampm;

                                                    // Obtiene la información del almacén seleccionado.
                                                    //var almacenSeleccionado = $('#almacen option:selected').text();
                                                    //var nombreAlmacen = almacenSeleccionado.includes('-') ? almacenSeleccionado : 'SinNombre';

                                                    // Obtiene las fechas de inicio y fin seleccionadas y las formatea.
                                                    //var fechaInicio = $('#fechaInicio').val().replace(/\//g, '-');
                                                    //var fechaFin = $('#fechaFin').val().replace(/\//g, '-');

                                                    // Formato del nombre del archivo: 'HOKA_REPORTE VENTA POR DEPARTAMENTO_NombreDelAlmacen_FechaInicio_FechaFin_FechaActual_HoraActual AM/PM.xlsx'
                                                    return 'HOKA JOYERIA_REPORTE ' + dateString + ' ' + timeString;
                                                },
                                                footer: {
                                                    columns: [3]
                                                }
                                            },


                                            {
                                                extend: 'pdf',
                                                className: 'btn btn-pdf rounded-0',
                                                text: '<i class="fas fa-file-pdf"></i> PDF',
                                                title: null,
                                                pageSize: 'A2',  // Establece el tamaño de página a A3
                                                orientation: 'landscape',
                                                filename: function () {
                                                    // Obtiene la fecha actual en formato string.
                                                    var now = new Date();
                                                    var dateString = now.toISOString().slice(0, 10);

                                                    // Convierte la hora a formato de 12 horas con AM o PM.
                                                    var hours = now.getHours();
                                                    var minutes = now.getMinutes();
                                                    var ampm = hours >= 12 ? 'PM' : 'AM';
                                                    hours = hours % 12;
                                                    hours = hours ? hours : 12; // La hora '0' debería ser '12'.
                                                    minutes = minutes < 10 ? '0' + minutes : minutes;
                                                    var timeString = hours + ':' + minutes + ' ' + ampm;


                                                    // Obtiene la información del almacén seleccionado.
                                                    //var almacenSeleccionado = $('#almacen option:selected').text();
                                                    //var nombreAlmacen = almacenSeleccionado.includes('-') ? almacenSeleccionado : 'SinNombre';

                                                    // Formato del nombre del archivo: 'HOKA - REPORTE VENTA POR PRODUCTO_NombreDelAlmacen_Fecha.pdf'
                                                    return 'HOKA JOYERIA REPORTE - ' + dateString + ' ' + timeString;
                                                },
                                                customize: function (doc) {
                                                    doc.defaultStyle.fontSize = 14; //tamaño de las letras dentro de la tablas al exportar

                                                    // Obtiene la fecha y hora actuales.
                                                    var currentDateTime = new Date();
                                                    var currentDateTimeString = currentDateTime.toLocaleString();

                                                    // Obtiene el rango de fechas ingresado.
                                                    //var fechaInicio = document.getElementById('fechaInicio').value;
                                                    //var fechaFin = document.getElementById('fechaFin').value;
                                                    //var fechaRango = 'DEL ' + fechaInicio + ' AL ' + fechaFin;

                                                    // Obtiene la información del almacén seleccionado.
                                                    //var almacenSeleccionado = $('#almacen option:selected').text();
                                                    //var sucursal = 'SUCURSAL ' + almacenSeleccionado + ' - ' + fechaRango;

                                                    // Fija el título del reporte a "HOKA - VENTA POR PRODUCTO".
                                                    var tituloReporte = 'HOKA - REPORTE JOYERIA';
                                                    var reporteDelDia = 'REPORTE DEL DÍA ' + currentDateTimeString;

                                                    // Agrega el título en la parte superior del documento con texto en negrita.
                                                    doc.content.splice(0, 0, {
                                                        text: tituloReporte,
                                                        alignment: 'center',
                                                        margin: [0, 0, 20, 20],
                                                        fontSize: 24,
                                                        bold: true  // Texto en negrita
                                                    });

                                                    // Agrega la fecha del reporte y la información del almacén en la misma línea, alineados a la izquierda y a la derecha, respectivamente.
                                                    doc.content.splice(1, 0, {
                                                        columns: [
                                                            {
                                                                text: reporteDelDia,
                                                                alignment: 'left',
                                                                fontSize: 18
                                                            },
                                                            //{
                                                            //    text: sucursal,
                                                            //    alignment: 'right',
                                                            //    fontSize: 18
                                                            //}
                                                        ],
                                                        columnGap: 10  // Ajusta el espacio entre las columnas.
                                                    });
                                                },
                                                footer: {
                                                    columns: [3]
                                                },
                                            }
                                            ],

                                        columns: [
                                            { data: 'folio_remision' },
                                            { data: 'product_code' },
                                            { data: 'description' },
                                            { data: 'pax' },
                                            { data: 'hotel' },
                                            { data: 'agency_name' },
                                            { data: 'guide' },
                                            { data: 'reference' },
                                            {
                                                data: 'total',
                                                render: function (data, type, row) {
                                                    return parseFloat(data).toLocaleString('es-MX', { style: 'currency', currency: 'MXN' });
                                                }
                                            },
                                            {
                                                data: 'x_date',
                                                render: function (data, type, row) {
                                                    return moment(data).utc().format('DD/MM/YYYY');
                                                }
                                            },

                                        ],

                                        deferRender: true, // Se habilita el renderizado diferido

                                        drawCallback: function (settings) {

                                        },
                                        footerCallback: function (row, data, start, end, display) {
                                            var api = this.api();
                                            var total = api.column(8, { page: 'current' }).data().reduce(function (a, b) { return a + (parseFloat(b) || 0); }, 0)

                                            total = total.toLocaleString('es-MX')
                                            $(api.column(8).footer()).html('Total: $' + total)

                                        },

                                    });

                                    $filtrarBusqueda.on('submit', function (event) {
                                        event.preventDefault();

                                        var almacen = $('select[name=almacen]').val();

                                        var fechaInicio = $('input[name=fechaInicio]').val();
                                        var fechaFin = $('input[name=fechaFin]').val();

                                        var hotel = $('select[name=hotel]').val();
                                        var agencia = $('select[name=agencia]').val();
                                        var guia = $('select[name=guia]').val();

                                        console.log(guia)

                                        // Validar campos
                                        //if (store =="") {
                                        //    Swal.fire({
                                        //        icon: 'error',
                                        //        title: 'Ups...',
                                        //        text: 'Por favor, complete todos los campos antes de realizar la búsqueda.',
                                        //    });
                                        //    return;
                                        //}

                                        var formData = { fechaInicio: fechaInicio, fechaFin: fechaFin, agencia: agencia, hotel: hotel, guia: guia, almacen: almacen }

                                        $.ajax({
                                            url: '/AdminPages/GetTicketsByStore',
                                            type: 'POST',
                                            data: formData,
                                            success: function (data) {
                                                // Comprobar si los datos están vacíos
                                                if (!data || data.length === 0) {
                                                    Swal.fire({
                                                        icon: 'error',
                                                        title: 'No se han registrado datos para este filtro',
                                                        text: 'Seleccione los campos donde si haya datos y vuelva a intentar',
                                                    });

                                                    $tablaRemisiod.DataTable().clear().draw();

                                                } else {
                                                    // Actualizar los datos de la tabla con la respuesta del servidor
                                                    $tablaRemisiod.DataTable().clear().rows.add(data).draw();

                                                }
                                            },
                                            error: function (jqXHR, textStatus, errorThrown) {
                                                console.log("jqXHR", jqXHR)
                                                console.log("textStatus", textStatus)
                                                console.log("errorThrown", errorThrown)
                                                alert('Error al filtrar/verificar filtrobusqueda');
                                            }
                                        });
                                    });

                                    $tablaRemisiod.on('length.dt', function (e, settings, len) {

                                    });

                                });

                            </script>
                        }
                    </div>
                </div>
            </section>
        </div>
    </div>
</body>
</html>

